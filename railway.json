{
  "build": {
    "buildCommand": "pip install 'setuptools>=68.0.0' && pip install --no-build-isolation -r requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
